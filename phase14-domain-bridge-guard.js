document.addEventListener('click',event=>{const button=event.target.closest?.('[data-decision="approve"]');if(!button)return;const card=button.closest?.('.p14-action');if(!card)return;const actionType=Array.from(card.querySelectorAll('.p14-badges>span')).map(x=>String(x.textContent||'').trim().toLowerCase()).join(' ');if(actionType.includes('attach client domain to vercel')){event.preventDefault();event.stopPropagation();event.stopImmediatePropagation();alert('Vercel domain attachment is still an external bridge step. The connected Vercel integration does not currently expose project-domain write access, so this approval stays pending instead of being consumed without execution.');return}if(actionType.includes('activate single customer pilot')){event.preventDefault();event.stopPropagation();event.stopImmediatePropagation();const actionId=card.dataset.action;if(!actionId){alert('Pilot activation action ID is missing. Nothing was activated.');return}import('./phase14-pilot-approval-executor.js').then(module=>module.approvePilotAction(actionId)).catch(error=>{console.error(error);alert('Pilot approval executor could not load. Nothing was activated.')});}},true);
import('./phase14-pilot-control.js').catch(error=>console.error('Pilot Control failed to load',error));
